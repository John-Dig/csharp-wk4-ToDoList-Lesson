@{
  Layout = "_Layout";
}

@using ToDoList.Models;

<h1>Items</h1>

@if (@Model.Count == 0)
{
  <h3>No items have been added yet!</h3>
} 
else
{
  @* <ul>
    @foreach (Item item in Model)
    {
      <li>@Html.ActionLink($"{item.Description}", "Details", new { id = item.ItemId }) | @item.Category.Name | @item.JoinEntities[0].Tag.Title</li>
    }
  </ul> *@<p>
@Html.ActionLink($"{Model[0].Description}", "Details", new { id = Model[0].ItemId }) | @Model[0].Category.Name | @Model[0].JoinEntities[0].Tag.Title</p>

}

<p>@Html.ActionLink("Add new item", "Create")</p>
<p>@Html.ActionLink("Home", "Index", "Home")</p>